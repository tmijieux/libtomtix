#ifndef INSERTION_SORT_H
#define INSERTION_SORT_H

#include <stdlib.h>

void cinsertion_sort(size_t n, char tab[]);
void iinsertion_sort(size_t n, int tab[]);
void finsertion_sort(size_t n, float tab[]);
void dinsertion_sort(size_t n, double tab[]);

#endif //INSERTION_SORT_H
